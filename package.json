{
  "name": "celine-root",
  "version": "1.0.0",
  "private": true,

  "workspaces": [
    "backend",
    "frontend"
  ],

  "scripts": {
    "start": "npm start --prefix backend",

    "heroku-postbuild": "npm install --prefix frontend && npm run build --prefix frontend && rm -rf backend/public && mkdir backend/public && cp -a frontend/dist/. backend/public/"
  },

  "engines": {
    "node": "20.x"
  }
}
